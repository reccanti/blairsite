import { sprinkles } from "@/themes/sprinkles.css";
import { style } from "@vanilla-extract/css";

export const headerLink = sprinkles({ padding: "spacing1" });
export const header = style({
  textTransform: "initial",
});
